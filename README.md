# vs-code-extension-skeleton-generator-using-docker
A Dockerized solution to generate a VS Code extension template using the Yeoman generator without needing to install Node.js, npm, or Yeoman locally.
